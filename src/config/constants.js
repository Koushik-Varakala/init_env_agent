export const DEMO_MODE = false; // set true to force demo conditions

export const AQI_THRESHOLDS = {
  GOOD: 50,
  MODERATE: 100,
  UNHEALTHY: 150
};

export const WIND_THRESHOLDS = {
  LOW: 2 // m/s considered low wind
};

export const PROFILES = {
  GENERAL: "general",
  ASTHMA: "asthma",
  ELDERLY: "elderly"
};

export const POLL_INTERVAL_MS = 60 * 1000; // 60s polling
